services:
  - type: web
    name: r-tam
    runtime: node
    buildCommand: npm install && cd backend && npm run build
    startCommand: npm start
envVars:
  - key: PORT
    value: 10000
  - key: NODE_ENV
    value: production
